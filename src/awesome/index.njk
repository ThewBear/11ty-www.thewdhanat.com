---
layout: base.njk
title: Awesome Thew
description: Awesome tools / resources curated by Thew Dhanat for programmer, web developer, software engineer, data scientist, and machine learning engineer.
---

<header class="w-full sm:w-4/5 xl:w-3/5 mx-auto my-4 px-4">
  <h1 class="text-4xl font-bold mb-2">Awesome Thew</h1>
  <p class="mb-2">
    Awesome tools / resources curated by
    <strong><a class="underline" href="/">Thew Dhanat</a></strong
    >.
  </p>
  <p class="mb-2">
    Inspired by
    <a class="underline" href="https://www.swyx.io/writing/learn-in-public/"
      >Learn In Public</a
    >,
    <a class="underline" href="https://github.com/sw-yx/spark-joy">spark-joy</a>
    and
    <a class="underline" href="https://github.com/wesbos/awesome-uses">uses</a>.
  </p>
</header>
<main class="w-full sm:w-4/5 xl:w-3/5 mx-auto mb-4 px-4">
  <section class="">
    <h2 id="contents" class="text-3xl font-bold">Contents</h2>
    <div class="flex flex-wrap my-2 space-x-4">
      {% for title in contents.keys() %}
      <a
        class="bg-black text-white py-2 px-4 hover:bg-gray-700"
        href="#{{ title | slug }}"
      >
        {{ title }}
      </a>
      {% endfor %}
    </div>
  </section>
  {%- for key, content in contents -%}
  <section id="{{ key | slug }}" class="my-8">
    <h1 class="text-3xl font-semibold mb-2">{{ key }}</h1>
    <div class="border-l-4 rounded border-black border-solid p-2">
      {%- for subcontent in content -%}
      <h2 class="text-2xl font-medium mb-2">{{ subcontent.title }}</h2>
      <p class="mb-2">{{ subcontent.description }}</p>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-4">
        {%- for item in subcontent.items -%}
        <div class="relative flex justify-end">
          <div class="rounded-xl overflow-hidden w-1/2 m-2 shadow-md">
            <a
              class="block h-28"
              href="{{ item.url }}"
              aria-labelledby="{{ item.title | slug }}"
            >
              {% urlThumbnail item.url %}
            </a>
          </div>
          <div class="w-1/2">
            <a
              href="{{ item.url }}"
              class="text-blue-900 hover:text-blue-800 hover:underline"
            >
              <h3 class="font-medium text-lg" id="{{ item.title | slug }}">
                {{ item.title }}
              </h3>
            </a>
            <p>{{ item.description }}</p>
          </div>
        </div>
        {%- endfor -%}
      </div>
      {%- endfor -%}
    </div>
  </section>
  {%- endfor -%}
  <p class="my-12 text-center text-xl">
    {% twemoji "🚧" %} Work in progress {% twemoji "🚧" %}
  </p>
  <aside>
    {% include "speedlify-score.njk" %}
  </aside>
</main>
